name: Android CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Cache
      id: cache
      uses: actions/cache@v3
      env:
        cache-name: cache
      with:
        key: ${{ runner.os }}-build-${{ env.cache-name }}
        path: |
            ~/.m2
            ~/.cargo
            ~/.gradle
            ~/.rustup
            ~/work
            ~/app
            !~/app/.*
            !~/app/*.*
            ~/app/lint-baseline.xml
            !~/app/src
            !~/app/rs/src
            !~/app/rs/.*
            !~/app/rs/*.*
    - name: Cargo install toolchain aarch64
      uses: actions-rs/toolchain@v1
      with:
         toolchain: stable
         target: aarch64-linux-android
         override: true
    - name: Cargo Setup aarch64
      uses: actions-rs/cargo@v1
      with:
         use-cross: true
    - name: Cargo install cargo-binstall
      run: cargo install cargo-binstall
    - name: Cargo install cross
      run: cargo binstall --no-confirm --no-symlinks cross
    - name: Cargo binstall --no-confirm --no-symlinks ndk
      run: cargo binstall --no-confirm --no-symlinks cargo-ndk
    - name: Cargo install xdk
      run: cargo binstall --no-confirm --no-symlinks cargo-xdk
    - name: Rustup add aarch64
      run: rustup target add aarch64-linux-android
    - name: Cargo Update
      run: cargo-update
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Gradlew check cargo
      run: ./gradlew cargoBuild
    - name: Build with Gradle
      continue-on-error: true
      run: ./gradlew build
    - name: Build Artifacts Twoyi-release.apk
      uses: actions/upload-artifact@v3
      with:
        path: ~/
    #- name: Build Artifacts Twoyi-debug.apk
      #uses: actions/upload-artifact@v3
      #with:
        #path: ~/app/build/outputs/apk/debug/*.apk
