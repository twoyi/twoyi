name: Android CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  arch: aarch64
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      target: ${{ env.arch }}-linux-android
      binstall-args: --no-confirm --no-symlinks
    steps:
    - uses: actions/checkout@v3
    - name: Cache
      id: cache
      uses: actions/cache@v3
      env:
        cache-name: cache
      with:
        key: ${{ runner.os }}-build-${{ env.cache-name }}
        path: |
          ~/.m2
          ~/.cargo
          ~/.gradle
          ~/.rustup
          ~/work
          ~/app
          !~/app/.*
          !~/app/*.*
          !~/app/src
          !~/app/rs/src
          !~/app/rs/.*
          !~/app/rs/*.*
          ~/app/lint-baseline.xml
    - name: Cargo install toolchain ${{ env.arch }}
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        target: ${{ env.target }}
        override: true
        default: true
    - name: Rustup add ${{ env.arch }}
      run: rustup target add aarch64-linux-android
    - name: Cargo install cargo-binstall
      run: cargo install cargo-binstall
    - name: Cargo binstall cargo-update
      run: cargo binstall ${{ env.binstall-args }} cargo-update
    - name: Cargo binstall cross
      run: cargo binstall ${{ env.binstall-args }} cross
    #- name: Cargo Setup ${{ env.arch }}
      #uses: actions-rs/cargo@v1
      #with:
         #use-cross: true
         #command: 
         #args: --target aarch64-linux-android 
    - name: Cargo binstall ndk
      run: cargo binstall ${{ env.binstall-args }} cargo-ndk
    - name: Cargo binstall xdk
      run: cargo binstall ${{ env.binstall-args }} cargo-xdk
    - name: Cargo Update
      run: cargo update && cargo install-update -a && cargo cargo-update
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Gradlew check cargo
      run: ./gradlew cargoBuild
    - name: Build with Gradle
      continue-on-error: true
      run: ./gradlew build
    - name: Build Artifacts Twoyi-release.apk
      uses: actions/upload-artifact@v3
      with:
        path: ~/
    #- name: Build Artifacts Twoyi-debug.apk
      #uses: actions/upload-artifact@v3
      #with:
        #path: ~/app/build/outputs/apk/debug/*.apk
